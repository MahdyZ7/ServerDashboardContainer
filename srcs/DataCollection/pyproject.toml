[project]
name = "datacollection"
version = "0.1.0"
description = "SSH-based data collection service for server monitoring"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psycopg2-binary==2.9.10",
    "python-dotenv==1.1.1",
]
